namespace Program;

public class Address(string city, string houseNum, string postalCode)
{
    public string City = city;
    public string HouseNum = houseNum;
    public string PostalCode = postalCode;
}